# =================================================================
# Docker Image Build & Publish to GHCR
# =================================================================
# This workflow builds the Corelix Platform image
# and publishes it to GitHub Container Registry (ghcr.io)
#
# =================================================================

name: Corelix.io PaaS - Build and Publish Docker Image

on:
  push:
    branches:
      - main
      - master
    paths:
      - 'src/**'
      - 'config/**'
      - 'database/**'
      - 'resources/**'
      - 'routes/**'
      - 'docker/**'
      - 'composer.json'
      - '.github/workflows/docker-build-publish.yml'

  release:
    types: [published]

  schedule:
    # Weekly rebuild every Monday at 07:00 UTC (picks up new Coolify base image releases)
    - cron: '0 7 * * 1'

  workflow_dispatch:
    inputs:
      coolify_version:
        description: 'Coolify version to build against'
        required: true
        default: 'latest'
        type: string
      push_image:
        description: 'Push image to registry'
        required: true
        default: true
        type: boolean

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  EDITION: ${{ github.repository == 'amirhmoradi/coolify-enhanced' && 'free' || 'pro' }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write

    strategy:
      fail-fast: false
      matrix:
        coolify_version:
          - latest
        include:
          - coolify_version: latest
            tags_suffix: ""

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Determine Coolify version
        id: coolify-version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "version=${{ github.event.inputs.coolify_version }}" >> $GITHUB_OUTPUT
          else
            echo "version=${{ matrix.coolify_version }}" >> $GITHUB_OUTPUT
          fi

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            # For releases: use release tag (e.g., v1.0.0)
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            # For main/master branch: use 'latest' tag
            type=raw,value=latest,enable=${{ github.ref == format('refs/heads/{0}', 'main') || github.ref == format('refs/heads/{0}', 'master') }}
            # Coolify version specific tags
            type=raw,value=coolify-${{ steps.coolify-version.outputs.version }},enable=true
            # SHA-based tag for traceability
            type=sha,prefix=sha-
          labels: |
            org.opencontainers.image.title=Corelix Platform ${{ env.EDITION }}
            org.opencontainers.image.description=Extends Coolify v4 with granular permissions, encrypted backups, network management, and more
            org.opencontainers.image.vendor=${{ github.repository_owner }}
            dev.coolify.base-version=${{ steps.coolify-version.outputs.version }}
            dev.coolify.edition=${{ env.EDITION }}

      - name: Build and push Docker image
        id: push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./docker/Dockerfile
          push: ${{ github.event_name != 'workflow_dispatch' || github.event.inputs.push_image == 'true' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            COOLIFY_VERSION=${{ steps.coolify-version.outputs.version }}
            PAAS_BRAND_NAME=${{ vars.PAAS_BRAND_NAME || 'Corelix Open' }}
            PAAS_BRAND_DESCRIPTION=${{ vars.PAAS_BRAND_DESCRIPTION || 'An open-source & self-hostable Heroku / Netlify / Vercel alternative' }}
            PAAS_DOCS_URL=${{ vars.PAAS_DOCS_URL || 'https://docs.corelix.io/' }}
            PAAS_SUPPORT_URL=${{ vars.PAAS_SUPPORT_URL || 'https://corelix.io/contact' }}
            PAAS_WEBSITE_URL=${{ vars.PAAS_WEBSITE_URL || 'https://corelix.io' }}
            PAAS_OG_IMAGE=${{ vars.PAAS_OG_IMAGE || '' }}
            PAAS_TWITTER_HANDLE=${{ vars.PAAS_TWITTER_HANDLE || '@corelixio' }}
            PAAS_BRAND_COLOR=${{ vars.PAAS_BRAND_COLOR || '' }}
            PAAS_HIDE_SPONSORSHIP=${{ vars.PAAS_HIDE_SPONSORSHIP || 'false' }}
            PAAS_HIDE_VERSION_LINK=${{ vars.PAAS_HIDE_VERSION_LINK || 'false' }}
            PAAS_HIDE_ANALYTICS=${{ vars.PAAS_HIDE_ANALYTICS || 'true' }}
            PAAS_WHITELABEL=${{ vars.PAAS_WHITELABEL || 'false' }}
          platforms: linux/amd64,linux/arm64
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: mode=max
          sbom: true

      - name: Generate artifact attestation
        if: ${{ github.event_name != 'workflow_dispatch' || github.event.inputs.push_image == 'true' }}
        continue-on-error: true
        uses: actions/attest-build-provenance@v2
        with:
          subject-name: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true

      - name: Output image details
        run: |
          echo "## Docker Image Published! :whale:" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Image Details" >> $GITHUB_STEP_SUMMARY
          echo "- **Registry:** ${{ env.REGISTRY }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Image:** ${{ env.IMAGE_NAME }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Edition:** ${{ env.EDITION }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Coolify Base Version:** ${{ steps.coolify-version.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Digest:** ${{ steps.push.outputs.digest }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Tags" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "${{ steps.meta.outputs.tags }}" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Usage" >> $GITHUB_STEP_SUMMARY
          echo '```bash' >> $GITHUB_STEP_SUMMARY
          echo "docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY

  # Build against multiple Coolify versions on release
  build-multi-version:
    if: github.event_name == 'release'
    runs-on: ubuntu-latest
    needs: build-and-push

    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write

    strategy:
      fail-fast: false
      matrix:
        coolify_version:
          # Add specific Coolify versions to build against here
          # These are common stable versions - update as needed
          - "4.0.0-beta.470"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract release version
        id: release
        run: echo "version=${{ github.event.release.tag_name }}" >> $GITHUB_OUTPUT

      - name: Build and push Docker image
        id: push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./docker/Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.release.outputs.version }}-coolify-${{ matrix.coolify_version }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:coolify-${{ matrix.coolify_version }}
          labels: |
            org.opencontainers.image.title=Corelix Platform
            org.opencontainers.image.description=Extends Coolify v4 with enhanced user role and project-level access management
            org.opencontainers.image.version=${{ steps.release.outputs.version }}
            dev.coolify.base-version=${{ matrix.coolify_version }}
            dev.coolify.edition=${{ env.EDITION }}
          build-args: |
            COOLIFY_VERSION=${{ matrix.coolify_version }}
          platforms: linux/amd64,linux/arm64
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Generate artifact attestation
        continue-on-error: true
        uses: actions/attest-build-provenance@v2
        with:
          subject-name: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true
